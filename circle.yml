general:
  branches:
    ignore:
      - test-server
      - staging
      - production
machine:
  node:
    version: 5.11.0
  environment:
    NODE_ENV: circleci
  services:
    - postgresql
deployment:
  staging:
    branch: staging
    heroku:
      appname: opencollective-staging-api
