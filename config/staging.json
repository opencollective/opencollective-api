{
  "database": {
    "options": {
      "port": 5432,
      "dialect": "postgres",
      "protocol": "postgres",
      "logging": true,
      "dialectOptions": {
        "ssl": true
      }
    }
  },
  "host": {
    // TODO: Does this need to be changed?
    "api": "https://opencollective-staging-api.herokuapp.com"
  },
  "stripe": {
    "redirectUri": "https://opencollective-staging-api.herokuapp.com/stripe/oauth/callback"
  }
}
