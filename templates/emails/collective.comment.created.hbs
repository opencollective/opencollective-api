{{#if expense }}
Subject: {{{collective.name}}}: New comment on expense {{{escapeForSubject expense.description}}} by {{{fromCollective.name}}}
{{else if conversation}}
Subject: New comment from {{{fromCollective.name}}} on "{{{escapeForSubject conversation.title}}}"
{{else if update}}
Subject: New comment from {{{fromCollective.name}}} on "{{{escapeForSubject update.title}}}"
{{else}}
Subject: {{{collective.name}}}: New comment
{{/if}}

{{> header}}

{{#if expense}}
  <p style="padding: 1em;">
    Hi,
    <br /><br />
    {{> linkCollective collective=fromCollective}} just posted
    a new comment on an Expense, <a href="{{permalink comment}}">{{expense.description}}</a>:
  </p>
{{else if conversation}}
  <p style="font-size: 17px; padding: 1em;">
    Hi {{collective.name}},
    <br /><br />
    {{> linkCollective collective=fromCollective}} just posted
    a new comment on a Conversation, <a href="{{permalink comment}}">{{conversation.title}}</a>:
  </p>
{{else if update}}
  <p style="font-size: 17px; padding: 1em;">
    Hi {{collective.name}},
    <br /><br />
    {{> linkCollective collective=fromCollective}} just posted
    a new comment on an Update, <a href="{{permalink comment}}">{{update.title}}</a>:
  </p>
{{/if}}


<table style="width: 100%; margin-bottom: 30px;">
  <tbody>
    <tr>
      <td style="border: 1px solid #e8edee; border-radius: 6px; padding: 2em 1em;">
        <blockquote style="color: #798088;font-size: 13px;text-align: left;padding: 0px 1em;margin: 0em 0;white-space: pre-line;">{{{comment.html}}}</blockquote>
      </td>
    </tr>
  </tbody>
</table>

<p>
  <center>
  {{#if expense}}
    <a href="{{permalink comment}}" class="btn blue">
      <div>Reply</div>
    </a>
  {{else}}
    <a href="{{permalink comment}}" class="btn blue"><div>View or Reply</div></a>
  {{/if}}
  </center>
</p>

{{> footer}}
