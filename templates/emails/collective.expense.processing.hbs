Subject: Payment being processed: {{{expense.description}}} for {{{collective.name}}}

{{> header}}

<center>
  <h3>{{> greeting}}</h3>
  <p>
    Your expense, <b><a href="{{permalink expense}}">{{expense.description}}</a></b>,
    submitted to {{> linkCollective collective=collective}} on {{moment expense.createdAt}}, is currently being processed by Wise and should be paid soon.
  </p>
</center>

<p>
  <center>
    <a href="{{permalink expense}}" class="btn blue">
      <div>View Expense</div>
    </a>
  </center>
</p>

{{> footer}}
