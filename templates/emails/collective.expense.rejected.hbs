Subject: Expense rejected: {{currency expense.amount currency=expense.currency}} for {{{collective.name}}}

{{> header}}

<p>Your expense, <a href="{{permalink expense}}">{{expense.description}}</a>, has been rejected.</p>

<table>
  <tr><th>Submitted by:</th><td>{{> linkCollective collective=fromCollective}}</td></tr>
  {{#if expense.privateMessage}}
    <tr><th valign="top">Note:</th><td>{{{expense.privateMessage}}}</td></tr>
  {{/if}}
</table>

<br />
<br />

<center>
  {{> expenseItems items=items}}
  <br /><br />

  <p>
    <center>
      <a href="{{permalink expense}}" class="btn blue">
        <div>View Expense</div>
      </a>
    </center>
  </p>

</center>

{{> footer}}
