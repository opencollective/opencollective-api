Subject: {{#ifCond expense.currency '===' 'USD'}}💵 {{/ifCond}}{{#ifCond expense.currency '===' 'EUR'}}💶 {{/ifCond}}{{#ifCond expense.currency '===' 'GBP'}}💷 {{/ifCond}}{{currency expense.amount currency=expense.currency}} paid from {{{collective.name}}} for {{{escapeForSubject expense.description}}}

{{> header}}

<style>
  th {
    text-align: right;
    padding-right: 10px;
  }
</style>

<center>
  <h3>{{> greeting}}</h3>
  <p>
    Your expense, <b><a href="{{permalink expense}}">{{expense.description}}</a></b>, submitted to {{> linkCollective collective=collective}} on {{moment expense.createdAt}} has been paid.
    {{#if expense.payoutMethod}}
      {{#ifCond expense.payoutMethod.type '===' 'BANK_ACCOUNT'}}
        The money can take a few days to arrive, depending on the bank.
      {{/ifCond}}
    {{else}}
      The money can take a few days to arrive, depending on the payment processor.
    {{/if}}
  </p>
  <h2><a href="{{permalink expense}}">{{currency expense.amount currency=expense.currency}} {{#ifCond transaction.hostCurrency '!==' expense.currency}}({{currency amountInHostCurrency currency=transaction.hostCurrency}}){{/ifCond}}</a></h2>
  <div><a href="{{permalink expense}}">{{transaction.description}}</a></div>
  <table>
    <tr>
      <th>Payee:</th><td>{{fromCollective.name}}</td>
    </tr>
    <tr>
      <th>Payout Method:</th><td>{{expense.payoutMethodLabel}}</td>
    </tr>
  </table>

  <br /><br />

    <center>
      <a href="{{permalink expense}}" class="btn blue">
        <div>View Expense</div>
      </a>
    </center>
  </p>

</center>

{{> footer}}
