coverage:
  status:
    patch:
      default:
        target: 100% # Makes sure all new code is tested
        informational: true # Don't fail the build if coverage is below target

ignore:
  - reports
  - migrations
  - eslint-rules
  - config

comment:
  behavior: once # update, if exists. Otherwise post new. Skip if deleted.
